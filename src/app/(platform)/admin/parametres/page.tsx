import { PlaceholderSection } from "@/components/common/placeholder-section";

export default function AdminSettingsPage() {
  return (
    <PlaceholderSection
      title="Paramètres Système"
      description="Configuration globale de la plateforme (paramètres, services e-mail/WhatsApp, sauvegardes). (Sprint 4)"
      sprint="Sprint 4"
    />
  );
}
