import { PlaceholderSection } from "@/components/common/placeholder-section";

export default function SettingsPage() {
  return (
    <PlaceholderSection
      title="Paramètres"
      description="Configuration de l'organisation : identité, préférences, modules, activités, notifications. (Sprint 1)"
      sprint="Sprint 1"
    />
  );
}
